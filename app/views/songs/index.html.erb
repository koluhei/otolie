<%= render "shared/header" %>
<h1>トップページ</h1>

<% @songs.each do |song| %>
  <h2><%= song.title %></h3>
  <p>クリエイター: <%= song.creator.name %></p>
  <%= audio_tag song.audio_url, controls: true, controlsList: "nodownload"%>
<% end %>
